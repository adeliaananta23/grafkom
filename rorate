using UnityEngine;

public class RotateObject : MonoBehaviour
{
    public float speed = 50f;

    void Update()
    {
        // Rotasi ke samping = rotasi pada sumbu Z
        transform.Rotate(0f, 0f, speed * Time.deltaTime);
    }
} 
